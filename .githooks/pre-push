#!/bin/sh

# Pre-push: full validation — the last gate before code hits the remote.
# Runs everything: lint, staleness checks, routing tests, smoke tests,
# Python suite, packaging validation.

set -eu

ROOT=$(git rev-parse --show-toplevel)
cd "$ROOT"

npm run check
